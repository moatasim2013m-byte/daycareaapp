# EMERGENT Builder Agent Rules

## Branch Setup
- MUST create and switch to a feature branch:
  - `git checkout -b emergent/<feature>`

## Delivery Requirements
- MUST stage and commit all changes:
  - `git add -A && git commit`
- MUST push branch updates:
  - `git push`
- MUST open a Pull Request.
